#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>

using namespace std;
int main ()
{
    int n;
    while (cin >> n && n){
        int most, least, c1, c2;
        vector <int> vect;
        for (int i = 0; i<n; i++){
            cin >> x;
            vect.push_back(x);
        }
        sort(vect.begin(), vect.end());
        least = vect[0] ;
        cout << c << endl;
    }
    return 0;
}
/// I have no freaking IDEA. what this shit is about
